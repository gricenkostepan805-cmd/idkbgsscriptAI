-- Script Path: game:GetService("ReplicatedStorage").StatReqs["Equip Collector"]
-- Took 0.09s to decompile.
-- Executor: Velocity (1.1.3)

-- https://lua.expert/
return {
	Description = function(p1) --[[ Description | Line: 6 ]]
		return nil
	end,
	Check = function(p1, p2) --[[ Check | Line: 13 ]]
		if not (p2 and p1) then
			return false
		end

		local EquippedCollector = p2.EquippedCollector

		if not EquippedCollector then
			return false
		end

		local Collectors = p1.Collectors

		if not Collectors then
			Collectors = { p1.Collector }
		end

		for i, v in ipairs(Collectors) do
			if v == EquippedCollector then
				return true
			end
		end

		return false
	end
}
